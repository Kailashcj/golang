package main

import "fmt"

const (
	HEIGHT = 4
	WIDTH  = 6
)

type pixel int //aliasing pixels as int
var screen [HEIGHT][WIDTH]pixel

func main() {
	for i := 0; i < HEIGHT; i++ {
		for j := 0; j < WIDTH; j++ {
			screen[i][j] = 0 //initialization of 2d array
		}
	}
	fmt.Println(screen)

}
